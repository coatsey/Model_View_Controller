<!-- Loop over each object in the projects array -->
{{#each entrys as |entry| }}
<div class="row mb-4 entry">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{{get_emoji}}}
      <a href="/enry/{{entry.id}}">{{entry.title}}</a>
    </h2>
    <p>
      <span for="img" aria-label="money">💲</span>
      <!-- Pass needed_funding value to the helper function -->
      <span class="dollar-amount">{{format_amount enrty.needed_funding}}</span>
      needed!
    </p>
    <p>Created by {{entry.user.name}} on {{format_date entry.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{entry.description}}
    </p>
  </div>
</div>
{{/each}}